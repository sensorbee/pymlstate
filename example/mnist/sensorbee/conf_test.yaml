network:
  listen_on: ':8090'
topologies:
  mnist:
    bql_file: 'test.bql'
logging:
  log_dropped_tuples:       true
  summarize_dropped_tuples: true
storage:
  uds:
    type: "fs"
    params:
      dir: "uds"
