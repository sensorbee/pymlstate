network:
  listen_on: ':15601'
topologies:
  mnist:
    bql_file: 'load_model.bql'
logging:
  log_dropped_tuples:       true
  summarize_dropped_tuples: true
storage:
  uds:
    type: "fs"
    params:
      dir: "uds"
